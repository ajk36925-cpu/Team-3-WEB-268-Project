## Install and run

1. Open a terminal in the `rolls-express` folder
2. Run:
   ```bash
   npm install
   ```
3. Seed the database from the existing JSON files:
   ```bash
   npm run seed:mongo
   ```
4. Start the app:
   ```bash
   npm start
   ```
5. Open:
   ```
   http://localhost:3000
   ```
